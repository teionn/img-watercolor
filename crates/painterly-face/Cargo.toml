[package]
name = "painterly-face"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "顔検出（UltraFace ONNX を tract で実行、pure Rust）"

[dependencies]
image = { version = "0.25", default-features = false, features = ["png", "jpeg"] }
tract-onnx = "0.21"
